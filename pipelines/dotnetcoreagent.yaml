kind: Pod
metadata:
  name: dotnetcore-build
spec:
  containers:
  - name: dotnetcore
    image: valorekhov/jnlp-slave-dotnet-core:2.0.0-sdk
    tty: true
    command:
    - cat
  nodeSelector:
    agentpool: linage
